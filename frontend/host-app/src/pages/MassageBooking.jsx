import React from 'react';
import { Box, Container, Typography } from '@mui/material';

export default function MassageBooking() {
  return (
    <Container sx={{ pt: 20 }}>
      <Typography variant="h4">Бронирование массажа</Typography>
      <Typography>Здесь будет панель выбора времени (в разработке)...</Typography>
    </Container>
  );
}